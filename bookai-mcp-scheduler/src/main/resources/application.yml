server:
  port: 8082

spring:
  application:
    name: bookai-mcp-scheduler
  datasource:
    url: jdbc:h2:file:./data/scheduler
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: false
